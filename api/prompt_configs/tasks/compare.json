{
  "name": "compare",
  "template": "Task template - compare:\nTopic: {topic}\nCompare two options explicitly. Use this required structure and labels: Option A, Option B, Key differences, When to choose A, When to choose B, Recommendation. Include at least two comparison dimensions (e.g., performance, complexity, cost, risk). Every tradeoff must be specific and actionable. Do not give a generic explanation of the topic; the output must be a comparison.",
  "metadata": {"output": "markdown"}
}
