# DepthAPI Ingest-time dependencies (NOT for production API)
# Install: pip install -r scripts/ingest_corpus/requirements-ingest.txt

# Web scraping (Fetcher runtime requires the "fetchers" extra)
scrapling[fetchers]==0.4.7

# PDF extraction (requires Java 11+ — install from adoptium.net)
opendataloader-pdf==2.4.1

# AST-aware code boundary detection (Phase 2)
tree-sitter>=0.24.0
tree-sitter-python>=0.23.0
tree-sitter-javascript>=0.23.0
tree-sitter-typescript>=0.23.0

# These are already in api/requirements.txt but listed here for standalone use
httpx>=0.27.0
